export type UserLogin = {
  email: string;
  password: string;
};

export type UserRegister = {
  email: string;
  phone: string;
  password: string;
};

export type Profile = {
  email: string;
  phone: string;
  last_login: string;
};
