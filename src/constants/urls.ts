export const baseURL = 'http://localhost:8000/api';

const deliveries = '/deliveries';
const auth = '/auth';

export const urls = {
  deliveries: { base: deliveries },
  auth: {
    login: auth,
  },
};
